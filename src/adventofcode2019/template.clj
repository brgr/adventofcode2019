(ns adventofcode2019.template
  (:require [clojure.string :as str]
            [clojure.core.match :refer [match]]
            [clojure.math.numeric-tower :as math]
            [clojure.set :as set])
  (:use [adventofcode2019.core]))



(defn -main []
  (let [input (slurp "resources/day.input")]
    (do
      (println "TODO"))))
